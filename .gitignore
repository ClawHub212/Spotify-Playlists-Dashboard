# Environment and credentials
.env
.cache
config.json

# Persisted playlist-track cache (runtime state, rewritten by the backend)
playlist_cache.json

# Legacy CSV data (now replaced by config.json)
data/csv/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/
venv/
.venv/
# scripts/dashboard-open.sh links .venv into a worktree as a SYMLINK, which the
# directory-only pattern above doesn't match
.venv
env/

# Claude Code worktrees
.claude/worktrees/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
