-- Toggle Spotify Dashboard (old) visibility (Tracker page)
-- If visible on Tracker page -> hides
-- If hidden or on another page -> shows Tracker page

tell application "Spotify Dashboard (old)"
    toggle page "tracker"
end tell
